# Demo folder

Place a paper PDF here as `paper.pdf`, then run:

```bash
paper2repro run examples/demo/paper.pdf --no-llm -o outputs/demo
```

The generated report and starter project will appear in `outputs/demo/`.
